#lang racket

(require sicp)

; (fibs n) will require n - 1 of additions since we don't have to compute
; values from the start and don't have to compute them after first time

; if we had not memoized procedure then
; nth fibs will require fib(n) additions















